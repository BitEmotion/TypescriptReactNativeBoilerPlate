import React, { useState } from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
    TouchableOpacity
} from 'react-native';

const ContainerScreen = () => {
    return (
        <>
        </>
    )
}

export default ContainerScreen;
